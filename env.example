# Option 1: NEAR AI Cloud (Managed TEE Service) - Recommended for production
# Get your API key from https://cloud.near.ai
NEARAI_API_KEY=your_api_key_here

# Option 2: Local TEE (Self-Hosted using private-ml-sdk)
# Uncomment and configure these if using a local TEE endpoint:
# TEE_API_URL=http://localhost:8000/v1
# TEE_API_TOKEN=your_local_tee_token
# TEE_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct

# Optional: Google Maps API Key (for better location autocomplete)
# If not set, the app uses OpenStreetMap (free, no API key needed)
# Get your key from https://console.cloud.google.com/google/maps-apis
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

